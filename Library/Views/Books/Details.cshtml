@{
  Layout = "_Layout";
}

@model Library.Models.Book

<h2>Book Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Title): @Html.DisplayFor(model => model.Title)</h3>

@if(@Model.Patrons.Count == 0)
{
  <p>This book does not belong to any patrons</p>
}
else
{
  <h4>Patrons the book belongs to:</h4>
  <ul>
  @foreach(var join in Model.Patrons)
  {
    <li>@join.Patron.Name</li>
  }
  </ul>
}

<p>@Html.ActionLink("Back to list", "Index")</p>
<p>@Html.ActionLink("Edit Book", "Edit", new { id = Model.BookId })</p>
<p>@Html.ActionLink("Delete Book", "Delete", new { id = Model.BookId })</p>
<p>@Html.ActionLink("Add a Patron", "AddPatron", new { id = Model.BookId })</p>
